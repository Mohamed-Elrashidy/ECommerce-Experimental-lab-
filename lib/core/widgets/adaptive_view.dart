import 'package:flutter/material.dart';

class AdaptiveView extends StatelessWidget {
   AdaptiveView({required this.mobile, required this.tablet});
  Widget mobile;
  Widget tablet;
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
